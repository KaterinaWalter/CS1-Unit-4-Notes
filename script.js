// This is a SINGLE LINE COMMENT

/* This is a MULTI LINE COMMENT
The code below is an example STATEMENT
(instruction/command)
Every statement ends with a SEMICOLON
 */
console.log("welcome to javascript!");

// VARIABLES are named storage containers for data (info)

// DECLARE a variable with the keyword LET
let username; 

// ASSIGN a value to a named variable
username = "ms.walter";

// DECLARE + ASSIGN a new variable in one line
let pokemon = "pikachu";

// See the value stored in a variable
console.log(username);
console.log(pokemon);

// Store numbers in a variable
let luckyNum = 13;
